import React, {Component} from 'react';

class FilterModal extends Component {
    render() {
        return (
            <div>
                FilterModal
            </div>
        );
    }
}

export default FilterModal;